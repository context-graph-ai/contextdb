[package]
name = "contextdb-cli"
version = "0.2.0"
edition = "2024"

[dependencies]
contextdb-engine = { path = "../contextdb-engine" }
rustyline.workspace = true
contextdb-core = { path = "../contextdb-core" }
